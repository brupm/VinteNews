.post
  %h3
    1.
    = link_to "↑", ""
    %strong
      = link_to post.title, post.url
    (
    = post.url
    )
  %p.mini
    submetido por
    = link_to post.user.login, user_path(user)
    há 
    %span.relative_post_date
      = post.created_at.to_formatted_s(:rfc822)
    atrás —
    %a{:href => "#"} #commentcount# comentários
